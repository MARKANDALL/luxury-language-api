# Backend Health Scan — 2026-05-31

## Summary
0 pool drift | 0 silent catches | 6 missing auth gate (INFO) | 0 CORS drift | 5 env var drift

## Top Priority
If you spend 15 minutes this week, fix the five missing entries in `.env.example` (image-generation feature shipped without doc updates) — first-time deployers will silently hit undefined keys.

## Findings

### 1. Postgres pool drift
✅ Clean

### 2. Silent catches
✅ Clean (44 catch blocks scanned; all log or return a response)

### 3. Missing auth gate (INFO — defense-in-depth only)
All 6 routes are gated at the router level via `api/router.js` `ADMIN_ONLY` (lines 251–263). Adding a per-handler check would harden against future router refactors but is not a live security hole.

- `routes/evaluate.js:63` — Azure Speech caller; no handler-level admin token check.
- `routes/assess.js:17` — Azure Speech caller; no handler-level admin token check.
- `routes/convo-image.js:44` — Gemini caller; no handler-level admin token check.
- `routes/voice-clone.js:7` — ElevenLabs caller; no handler-level admin token check.
- `routes/voice-mirror.js:7` — ElevenLabs caller; no handler-level admin token check.

### 4. CORS drift
✅ Clean (no route overrides or wildcards `Access-Control-Allow-Origin`; CORS is centrally managed by `api/router.js:208–248`)

### 5. Env var drift
Referenced in code but absent from `.env.example` (not even as a commented alias):

- `GEMINI_API_KEY` — `routes/convo-image.js:9,53`
- `LUMA_API_KEY` — `routes/convo-image-luma.js:50,64,109`
- `LUMA_IMAGE_MODEL` — `routes/convo-image-luma.js:8`
- `LUX_IMAGE_PROVIDER` — `routes/convo-image.js:48`
- `FRONTEND_URL` — `routes/convo-image.js:40`, `routes/convo-image-luma.js:11`

Reverse direction (`.env.example` entries unreferenced in code): none.

## Additional Findings
- `routes/convo-image-luma.js:106` — Luma caller; no handler-level admin token check (INFO — reachable only via `convo-image` delegation, which is router-gated).

## Legacy Backlog
✅ None (all flagged files modified within the 60-day window)

## Notes
- This week's in-scope churn: `routes/convo-turn.js` (commit `da11f7c` — maxDuration 60s + observability). Auth gate at `routes/convo-turn.js:574` intact; no regressions introduced.
- Canary observation (informational, no action requested): `api/router.js` was touched this week (same commit) to set `maxDuration: 60` at the function level. CORS, ADMIN_ONLY list, and lazy-load error envelope are unchanged.
- Pattern: every env-var-drift entry traces to the convo-image / Luma image-generation feature (`routes/convo-image*.js`). When that feature was added (~2 weeks ago), `.env.example` was not updated alongside it. Worth a one-time sweep, then call it done.
- Trend: no new silent-catch or pool-drift patterns introduced in this week's edits; the codebase continues to honor the shared-pool and logged-rethrow conventions.
