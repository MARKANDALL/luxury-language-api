# Backend Health Scan — 2026-04-23

## Summary
[0] pool drift | [0] silent catches | [3] missing auth gate (INFO) | [0] CORS drift | [0] env var drift

## Top Priority
None — all findings are advisory (router-level gating already covers the paid routes; in-handler checks would be defense-in-depth).

## Findings

### 1. Postgres pool drift
✅ Clean

### 2. Silent catches
✅ Clean

### 3. Missing auth gate on paid routes
All three routes below are gated at the router level via `api/router.js` ADMIN_ONLY (lines 247–258), so this is architectural defense-in-depth debt, **not** a live security hole. Reported as INFO.

- `routes/voice-mirror.js:7` — handler calls ElevenLabs via `synthesizeSpeech` with no per-handler admin token check. Modified 5 days ago.
- `routes/voice-clone.js:~` — handler calls ElevenLabs clone API with no per-handler admin token check. Modified 22 days ago.
- `routes/assess.js:~` — handler calls Azure Speech with no per-handler admin token check. Modified 54 days ago.

### 4. CORS drift
✅ Clean — CORS is centrally managed in `api/router.js:206–237`; no route file overrides or wildcards it.

### 5. Env var drift
✅ Clean — every `process.env.*` reference in code has a matching entry (or documented alias) in `.env.example`, and every uncommented entry in `.env.example` is referenced in code.

## Legacy Backlog
✅ None — no flaggable findings in files older than 60 days (the only such paid route, `routes/evaluate.js`, is a canary and is handled under Notes).

## Notes
- **Canary `routes/evaluate.js` (69 days old)**: like `voice-clone`/`voice-mirror`/`assess`, it has no per-handler ADMIN_TOKEN check and relies on router-level gating. Same defense-in-depth pattern; not flagged as a finding per canary policy.
- **Canary `api/router.js`**: ADMIN_ONLY set (lines 247–258) correctly enumerates all 10 paid routes. CORS preflight at line 240 returns 204 before auth gate, which is correct (browsers would otherwise surface misleading CORS errors).
- **Canary `lib/pool.js`**: all six DB-touching routes (`admin-recent`, `attempt`, `convo-report`, `migrate`, `update-attempt`, `user-recent`) import the shared singleton. No custom `new Pool(...)` instances anywhere in the tree.
- **Week's diff**: only `routes/voice-mirror.js` changed in the last 7 days (commit `87807a9`). The change converted a fire-and-forget `.then(() => {})` into a logged `.catch((err) => console.warn(...))` on the `last_used_at` update, plus added `.env.example`. This is a net quality improvement — one fewer silent-catch candidate.
- **Pattern observation**: the three voice/audio routes (`voice-clone`, `voice-mirror`, `assess`) are the only paid routes without in-handler ADMIN_TOKEN checks. If you ever harden in-handler auth as a batch, these three are the full set.
