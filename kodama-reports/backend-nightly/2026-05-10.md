# Backend Health Scan — 2026-05-10

## Summary
0 pool drift | 0 silent catches | 4 missing auth gate (INFO) | 0 CORS drift | 2 env var drift

## Top Priority
If you spend 15 minutes this week, document `GEMINI_API_KEY` and `FRONTEND_URL` in `.env.example` — `routes/convo-image.js` (added this week) hard-fails without `GEMINI_API_KEY` and a fresh contributor would have no signpost.

## Findings

### 1. Postgres pool drift
✅ Clean. Only `lib/pool.js` instantiates `new Pool(...)` and writes `globalThis.__lux_pool`. All routes that need DB access import the shared singleton.

### 2. Silent catches
✅ Clean. Every catch block in scope either logs (`console.warn`/`console.error`), returns an error response, rethrows, or returns a non-null fallback value (e.g. `convo-turn.js:292,296,600` repair paths return original/parsed-shape data, not null).

### 3. Missing auth gate on paid routes (INFO — defense-in-depth debt)
All routes below are gated at the router level via `api/router.js` `ADMIN_ONLY` (lines 249–261), so this is not a live security hole — it's per-handler hardening that drifted from the canonical pattern in `convo-turn.js:517–523`.

- `routes/convo-image.js:44` — calls Gemini (`ai.models.generateContent`) with no per-handler `ADMIN_TOKEN` check. (Modified 3 days ago, so worth aligning while the file is hot.)
- `routes/assess.js:17` — calls Azure Speech with no per-handler check.
- `routes/voice-clone.js:7` — calls ElevenLabs (`createVoiceClone`/`deleteVoiceClone`) with no per-handler check.
- `routes/voice-mirror.js:7` — calls ElevenLabs (`synthesizeSpeech`) with no per-handler check.

### 4. CORS drift
✅ Clean. Every route file relies on the central CORS handling in `api/router.js:208–246`. No route overrides `Access-Control-Allow-Origin` or wildcards it.

### 5. Env var drift
- `routes/convo-image.js:9,47` — `process.env.GEMINI_API_KEY` referenced; not present in `.env.example`.
- `routes/convo-image.js:40` — `process.env.FRONTEND_URL` referenced; not present in `.env.example`.

No reverse drift: every variable in `.env.example` (including commented aliases like `AZURE_TTS_KEY`, `DATABASE_URL`, `SUPABASE_SERVICE_ROLE`) is referenced somewhere in code.

## Legacy Backlog
✅ None. All flagged files were modified within the last 60 days.

## Notes
- **Week's commit shape:** 21 commits in the last 7 days, all on two files: `routes/convo-turn.js` (narration/tone/phase system) and `routes/convo-image.js` (image model swap, retry/fallback, shot direction). Both files have grown substantially — `convo-turn.js` is now 689 lines, mostly prompt scaffolding. Not a code-health issue today, but worth watching for extraction opportunities (e.g. `LEVEL_INSTRUCTIONS`, `TONE_INSTRUCTIONS`, the narration block) if the file crosses ~1000 lines.
- **Canary observations (informational only):** `api/router.js` `ADMIN_ONLY` set already includes both `convo-image` and `convo-turn`, so router-level gating for this week's hot files is intact. `lib/pool.js` is unchanged — singleton pattern still holds.
- **GEMINI_API_KEY** is the first new third-party key added since `.env.example` was created on 2026-04-18; the `.env.example` should be updated alongside any new external-service integration so contributor onboarding doesn't decay.
- The two `convo-turn.js` `catch { ... }` blocks at lines 292 and 600 reassign `json` rather than swallowing — they're the JSON-parse-with-fallback idiom, not silent catches. Flagged here pre-emptively in case future audits look suspicious at them.
