# Backend Health Scan — 2026-07-12

## Summary
0 pool drift | 1 silent catch | 5 missing auth gate (INFO) | 0 CORS drift | 5 env var drift

## Top Priority
If you spend 15 minutes this week, look at the `.env.example` gap for `GEMINI_API_KEY` and `LUX_IMAGE_PROVIDER` — `routes/convo-image.js` was rewritten 3 days ago to call Gemini and (optionally) route to Luma, but a fresh clone with `.env.example` copied to `.env` has no hint either variable exists.

## Findings

### 1. Postgres pool drift
✅ Clean — `lib/pool.js` remains the sole `new Pool(...)` call; no route creates its own pool or writes to `globalThis.__lux_pool`.

### 2. Silent catches
- `routes/convo-image.js:27` — `fetchImageAsBase64()` catches every `fetch` failure with a bare `return null;` (no `console.*`, no comment). Reference-image fetch failures for character portraits, video stills, and scene webp/jpg are indistinguishable from "no such asset" — a broken `FRONTEND_URL` or a 500 from the frontend host produces a silently degraded image with no log line.

### 3. Missing auth gate on paid routes
All five below are gated by `api/router.js`'s `ADMIN_ONLY` set, so this is architectural defense-in-depth debt (INFO), not a live security hole. Each route still calls a paid provider directly if invoked with the router bypassed (e.g. imported by another module or reached via a future non-router entrypoint).
- `routes/assess.js:17` (INFO) — Azure Speech; no handler-level `ADMIN_TOKEN` check.
- `routes/convo-image.js:44` (INFO) — Gemini (and Luma via delegation); no handler-level check.
- `routes/dictate.js:24` (INFO) — Azure Speech; no handler-level check.
- `routes/voice-clone.js:7` (INFO) — ElevenLabs voice cloning; no handler-level check.
- `routes/voice-mirror.js:7` (INFO) — ElevenLabs TTS; no handler-level check.

### 4. CORS drift
✅ Clean — every recently-touched route relies on the router's centralized CORS. No route wildcards or overrides `Access-Control-Allow-Origin`. (`routes/realtime-webrtc-session.js:46` adds `Access-Control-Expose-Headers` for a custom debug header — additive, not a policy override.)

### 5. Env var drift
Referenced in code but missing from `.env.example`:
- `GEMINI_API_KEY` — `routes/convo-image.js:9,53` (added 3 days ago; primary image provider).
- `LUX_IMAGE_PROVIDER` — `routes/convo-image.js:48` (added 3 days ago; toggles Gemini vs Luma).
- `FRONTEND_URL` — `routes/convo-image.js:40`, `routes/convo-image-luma.js:11` (used to fetch character portraits and scene stills).
- `LUMA_API_KEY` — `routes/convo-image-luma.js:50,64,109`.

`.env.example` entries referenced nowhere in code: none — every documented key is used.

## Additional Findings
- `routes/convo-image-luma.js:8` — env var drift: `LUMA_IMAGE_MODEL` referenced in code, absent from `.env.example`.

## Legacy Backlog
✅ None — every finding above lives in a file touched within the 60-day window (`convo-image-luma.js` was last modified ~58 days ago, still inside the backend legacy threshold).

## Notes
- Canary files touched this week: `api/router.js` (added `dictate` + `coach-explain` to the route table and the `ADMIN_ONLY` set), `lib/voice.js` (accepts a `pack` argument for es-MX synthesis). Both edits look additive and non-regressive.
- `routes/session-analyst.js:77` also matches the strict `return null;` silent-catch pattern, but it is inside `parseReport()`'s deliberate parse-then-repair fallback — the null propagates to a retry loop and the caller's outer catch (`line 297-299`) logs the eventual failure. Deliberately not flagged; noted for provenance.
- Two new pack-neutral dictionaries landed under `lang/session-analyst/{en,es}.js` — pure data modules, no routes, no env, no I/O; nothing to scan.
- Week-over-week: this is the first backend nightly report to land after `.env.example` was introduced (2026-04-18); five drift entries below is expected as the image-provider work (Gemini + Luma) predates the template and never got backfilled.
