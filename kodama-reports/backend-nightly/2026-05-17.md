# Backend Health Scan — 2026-05-17

## Summary
0 pool drift | 2 silent catches | 0 missing auth gate (HIGH) | 0 CORS drift | 5 env var drift

(Also: 5 INFO-level handler-side auth-gate observations — router-gated, defense-in-depth debt only.)

## Top Priority
If you spend 15 minutes this week, look at the env var drift — five new variables (`FRONTEND_URL`, `GEMINI_API_KEY`, `LUMA_API_KEY`, `LUMA_IMAGE_MODEL`, `LUX_IMAGE_PROVIDER`) added by the recent image-provider work are not documented in `.env.example`, so a fresh deploy will silently fall back to localhost or fail with a 500.

## Findings

### 1. Postgres pool drift
✅ Clean. All six DB-touching routes (`migrate`, `update-attempt`, `convo-report`, `admin-recent`, `user-recent`, `attempt`) import `pool` from `lib/pool.js`. No stray `new Pool(...)` and no direct `globalThis.__lux_pool` writes outside `lib/pool.js`.

### 2. Silent catches
- `routes/convo-image.js:26-28` — `catch { return null; }` in `fetchImageAsBase64`. Network/decode failures on reference-image fetches disappear silently; if Gemini reference loading degrades, there is no signal. Add a single `console.warn` with the URL.
- `routes/convo-image-luma.js:26-28` — identical `catch { return null; }` (copy of the Gemini helper). Same recommendation; failed Luma reference loads are invisible.

### 3. Missing auth gate on paid routes
✅ No HIGH findings. Every paid route is gated either at the handler or the router level.

INFO (router-gated, no per-handler check — defense-in-depth debt):
- `routes/assess.js:17` — Azure Speech, router-gated as `assess`.
- `routes/convo-image.js:44` — Gemini, router-gated as `convo-image`.
- `routes/convo-image-luma.js:106` — Luma, reached only via dispatch from `convo-image` (transitively gated).
- `routes/voice-clone.js:7` — ElevenLabs, router-gated as `voice-clone`.
- `routes/voice-mirror.js:7` — ElevenLabs, router-gated as `voice-mirror`.

### 4. CORS drift
✅ Clean. CORS is handled centrally in `api/router.js:208-246`. No route file sets `Access-Control-Allow-Origin` itself or wildcards the policy.

### 5. Env var drift
Referenced in code but missing from `.env.example`:
- `FRONTEND_URL` — `routes/convo-image.js:40`, `routes/convo-image-luma.js:11` (defaults to `http://localhost:3000`; will silently break image references in production).
- `GEMINI_API_KEY` — `routes/convo-image.js:9, 53` (handler returns 500 if missing, but it is undocumented).
- `LUMA_API_KEY` — `routes/convo-image-luma.js:50, 64, 109` (handler returns 500 if missing).
- `LUMA_IMAGE_MODEL` — `routes/convo-image-luma.js:8` (defaults to `uni-1-max`).
- `LUX_IMAGE_PROVIDER` — `routes/convo-image.js:48` (provider toggle; undocumented switch between Gemini and Luma).

Entries in `.env.example` referenced nowhere in code: none. All listed aliases are referenced.

## Legacy Backlog
✅ None. Every route file in scope was modified within the last 60 days.

## Notes
- All three recently-modified files (`routes/convo-image.js`, `routes/convo-image-luma.js`, `routes/convo-turn.js`) are non-canary; canaries untouched this week.
- The two silent catches flagged are the same helper copied between `convo-image.js` and `convo-image-luma.js`. When fixing one, fix both — they will likely drift further as the Luma path matures.
- The new Luma route file mirrors the Gemini file's shape closely (helpers, prompt scaffolding, dispatch). Worth keeping an eye on for divergence next week.
- Out of scope for the five checks but worth a glance: `routes/assess.js:133-145` `finally` block has two empty `if (inputPath) { }` blocks and a duplicated cleanup that calls `fs.rm(outputPath, ...)` where it should be `fs.rm(inputPath, ...)`. The `inputPath` temp file is never cleaned up. Not introduced this week (file untouched for 2 weeks) but a real leak.
