# Backend Nightly Health Scan — 2026-04-16

## Summary
0 pool drift | 1 silent catches | 3 missing auth gate | 0 CORS drift | 1 env var drift

## Findings

### 1. Postgres pool drift
✅ Clean — no route creates its own `new Pool(...)` or writes to `globalThis.__lux_pool`; all Postgres-using routes import from `lib/pool.js`.

### 2. Silent catches
- `routes/voice-mirror.js:57` — `.catch(() => {})` on the fire-and-forget `last_used_at` Supabase update swallows DB errors without logging. At minimum, a `console.warn` would surface persistent write failures.

### 3. Missing auth gate on paid routes
- `routes/voice-clone.js:7` — handler calls ElevenLabs (`createVoiceClone`/`deleteVoiceClone` via `lib/voice.js`) with no per-handler `ADMIN_TOKEN` check. (Router-level `ADMIN_ONLY` set in `api/router.js:247` does gate it, but this breaks the defense-in-depth pattern used by `pronunciation-gpt`, `alt-meaning`, `convo-turn`, `convo-report`, `tts`, `realtime-webrtc-session`.)
- `routes/voice-mirror.js:7` — handler calls ElevenLabs (`synthesizeSpeech` via `lib/voice.js`) with no per-handler `ADMIN_TOKEN` check. Same gate-via-router caveat as above.
- `routes/assess.js:17` — handler calls Azure Speech with no per-handler `ADMIN_TOKEN` check. Router gates it via `ADMIN_ONLY`, but per-handler check is missing.

### 4. CORS drift
✅ Clean — no route sets `Access-Control-Allow-Origin` (wildcard or otherwise); `api/router.js:206-244` owns the origin allowlist + OPTIONS 204 preflight for all dispatched routes.

### 5. Env var drift
- `.env.example` does not exist at repo root. Cannot cross-reference code references against documented env vars.

Env vars referenced in non-canary code (for future `.env.example`):
`ADMIN_TOKEN`, `OPENAI_API_KEY`, `AZURE_SPEECH_KEY`, `AZURE_TTS_KEY`, `AZURE_SPEECH_REGION`, `AZURE_REGION`, `ELEVENLABS_API_KEY`, `ENABLE_PROSODY`, `LUX_AI_QUICK_MODEL`, `LUX_AI_DEEP_MODEL`, `LUX_AI_DEEP_REASONING_MODEL`, `LUX_AI_DEEP_REASONING_EFFORT`, `LUX_AI_TRANSLATE_MODEL`, `LUX_AI_MODEL`, `LUX_AI_REPORT_MODEL`, `LUX_AI_CONVO_MODEL`.
(Canary files additionally reference: `POSTGRES_URL`, `POSTGRES_CONNECTION`, `DATABASE_URL`, `PGSSLMODE`, `SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_SERVICE_ROLE_KEY_JWT`, `SUPABASE_SERVICE_KEY`, `SUPABASE_ANON_KEY`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `CORS_ORIGINS`.)

## Notes
- Canary `api/router.js` is the sole choke point for CORS + admin gating (via the `ADMIN_ONLY` set at line 247). The three "missing auth gate" findings above are all in that set, so in practice they are protected; the finding is about defense-in-depth consistency, not an exploitable hole.
- Canary `lib/pool.js` remains the single Postgres pool singleton; `globalThis.__lux_pool` is only referenced by the canary itself and by `test/attempt.contract.test.js:8` (which resets the singleton between contract tests — expected).
- 24-hour commit window contained only one commit (`256efc4` — archive of routine prompts). No code churn in scope; findings above reflect the standing state, not newly-introduced regressions.
- Observation: `.catch(() => {})` appears only in `voice-mirror.js`. No other empty promise-chain catches exist in scope.
- Observation: OPTIONS 204 handling is present in 12 of 16 route handlers (inconsistent but benign — router handles OPTIONS before dispatch). Not flagged; noted for potential cleanup in a future weekly pass.
