# Backend Health Scan — 2026-05-03

## Summary
0 pool drift | 1 silent catch | 4 missing auth gate (INFO) | 0 CORS drift | 2 env var drift

## Top Priority
If you spend 15 minutes this week, look at the env var drift because two new vars (`GEMINI_API_KEY`, `FRONTEND_URL`) used by the freshly-added `convo-image` route never made it into `.env.example`, so any new env will hit silent runtime failures.

## Findings

### 1. Postgres pool drift
✅ Clean

### 2. Silent catches
- `routes/convo-image.js:25` — `fetchImageAsBase64` swallows fetch/parse errors with bare `return null;` (no log, no comment marking it intentional). Cache misses on transient network blips will be invisible.

### 3. Missing auth gate on paid routes
All four are gated at the router level via `api/router.js`'s `ADMIN_ONLY` set, so this is defense-in-depth debt, not a live security hole — reported as INFO.

- `routes/assess.js:17` — INFO. No per-handler `ADMIN_TOKEN` check; calls Azure Speech.
- `routes/convo-image.js:43` — INFO. No per-handler `ADMIN_TOKEN` check; calls Gemini (paid).
- `routes/voice-clone.js:7` — INFO. No per-handler `ADMIN_TOKEN` check; calls ElevenLabs.
- `routes/voice-mirror.js:7` — INFO. No per-handler `ADMIN_TOKEN` check; calls ElevenLabs.

### 4. CORS drift
✅ Clean — only `api/router.js` sets `Access-Control-Allow-Origin`; no individual route overrides.

### 5. Env var drift
- `routes/convo-image.js:8` references `process.env.GEMINI_API_KEY` — missing from `.env.example`.
- `routes/convo-image.js:39` references `process.env.FRONTEND_URL` — missing from `.env.example`.
- Reverse direction (`.env.example` entries unreferenced in code): ✅ none.

## Legacy Backlog
✅ None — all findings are on files modified within the last 60 days.

## Notes
- Heavy churn this week was concentrated in the new `convo-image` route (Gemini image generation, 6 commits in 7 days) and the convo-turn character-anchor v3 work. Both are net-new functionality, not refactors of canaries.
- Canary `api/router.js` was modified 12h ago to register `convo-image` and add it to `ADMIN_ONLY`. Wiring is consistent with the rest of the router (ADMIN_ONLY membership + lazy/eager route map). No drift in canary shape.
- Canary `routes/evaluate.js` was not touched in the 7-day window. No observations.
- Outside the scan checks but worth a glance: `routes/assess.js:134-145` has a malformed `finally` cleanup block — two `if (inputPath) {    }` empty blocks and the inputPath cleanup actually `fs.rm`s `outputPath` again. Temp `inputPath` files from formidable may be leaking. Not flagged as a finding under any check, surfaced here as a pattern observation.
- Three catches in `routes/convo-turn.js` (lines 221, 225, 431) and several in `tts/speak.js` recover silently into fallback values rather than logging. They do NOT match the strict silent-catch criteria (the body isn't bare `return null` / `return` / empty), so they are not flagged — but as a trend the convo-turn pipeline is leaning on quiet recovery in places where a one-line `console.warn` would help future debugging.
