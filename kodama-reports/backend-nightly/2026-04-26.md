# Backend Health Scan — 2026-04-26

## Summary
0 pool drift | 0 silent catches | 3 missing auth gate (INFO) | 0 CORS drift | 0 env var drift

## Top Priority
None — all findings are advisory.

## Findings

### 1. Postgres pool drift
✅ Clean

### 2. Silent catches
✅ Clean

### 3. Missing auth gate on paid routes
All three routes below are protected by the router-level `ADMIN_ONLY` set in `api/router.js:247-258`, so this is INFO-level defense-in-depth debt, not a live security hole.

- `routes/voice-mirror.js:21` (handler entry) — calls ElevenLabs; no per-handler admin token check. INFO.
- `routes/assess.js` (handler entry) — calls Azure Speech; no per-handler admin token check. INFO.
- `routes/voice-clone.js` (handler entry) — calls ElevenLabs + Supabase; no per-handler admin token check. INFO.

### 4. CORS drift
✅ Clean — all route files defer to the central CORS handler in `api/router.js:206-237`. No route explicitly overrides or wildcards `Access-Control-Allow-Origin`.

### 5. Env var drift
✅ Clean — all 29 `process.env.*` references in code have matching entries in `.env.example`, and every `.env.example` entry is referenced somewhere in code.

## Legacy Backlog
✅ None

## Notes
- **Activity gate**: Only one backend `.js` file changed in the last 7 days — `routes/convo-turn.js`. Recent commits cover a debug log of the active model (`7aa4bb1`), a save/deploy fix (`7c2c627`), and three behavioral prompt-engineering fixes (`dd4a3f3`, `b642cd7`, `d3eb052`).
- **convo-turn.js review**: Handler is correctly auth-gated at line 307–313, handles OPTIONS preflight at line 304, logs and 500s on outer catch at line 367. The three inner `catch` blocks (221, 225, 350) all provide fallback values rather than silently swallowing — by the strict silent-catch definition (empty `{}`, `return null;`, `return;`, or `/* ignore */`), none qualify.
- **Canary observation (informational only)**: `routes/evaluate.js` also has no per-handler admin token check. As a canary it is excluded from findings, but the same router-level ADMIN_ONLY protection applies, so the posture matches the three INFO findings above. If the team ever decides to harden per-handler auth across paid routes, evaluate.js should be in the same batch.
- **Trend**: This is the first scan in this report directory, so no week-over-week comparison is available yet.
