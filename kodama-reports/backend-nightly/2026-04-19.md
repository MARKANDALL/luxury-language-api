# Backend Health Scan — 2026-04-19

## Summary
0 pool drift | 0 silent catches | 3 missing auth gate (INFO) | 0 CORS drift | 0 env var drift

## Top Priority
None — all findings are advisory (defense-in-depth debt, not live security holes).

## Findings

### 1. Postgres pool drift
✅ Clean. `new Pool(...)` appears only in `lib/pool.js`; `globalThis.__lux_pool` is assigned there and referenced only by `test/attempt.contract.test.js:7-8` to reset the singleton between tests.

### 2. Silent catches
✅ Clean. Every `catch` and `.catch` either logs (`console.warn` / `console.error`), returns an error response, rethrows, or performs a documented recovery (e.g., `runCoach.js:128` falls back to `parseJsonWithRepair`). The new fire-and-forget `.catch` in `routes/voice-mirror.js:57` logs via `console.warn`.

### 3. Missing auth gate on paid routes
All findings below are **INFO** — each route is gated at the router level via `api/router.js` `ADMIN_ONLY` (lines 247-258), so there is no live auth bypass. These are defense-in-depth gaps (no per-handler check to match the canonical pattern used by `tts.js`, `pronunciation-gpt.js`, `convo-turn.js`, etc.).

- `routes/voice-mirror.js:7` — ElevenLabs `synthesizeSpeech` call with no handler-level admin-token check. (Modified 9h ago — new code inherits the pattern from `voice-clone.js`.)
- `routes/voice-clone.js:7` — ElevenLabs `createVoiceClone` / `deleteVoiceClone` calls with no handler-level admin-token check.
- `routes/assess.js:17` — Azure Speech (`AZURE_SPEECH_KEY` used at line 28) with no handler-level admin-token check.

### 4. CORS drift
✅ Clean. Only `api/router.js:226-228` sets `Access-Control-Allow-Origin`; no route handler overrides or wildcards CORS.

### 5. Env var drift
✅ Clean. Every `process.env.X` reference in code maps to an entry (active or commented alias) in `.env.example`, and every `.env.example` entry is referenced somewhere in code.

## Legacy Backlog
✅ None. Only three in-scope files exceed the 60-day cutoff (`routes/evaluate.js` — canary; `routes/tts.js` — auth-gated at handler; `routes/tts/ssml.js` — helper, not a handler), and none would otherwise be flagged.

## Notes
- **Activity:** Only one backend file changed in the last 7 days: `routes/voice-mirror.js` (commit `87807a9`, "fix(voice-mirror): log last_used_at update failures; add .env.example"). That commit converted a fire-and-forget `.then(() => {})` to a logged `.catch` — directly resolves a silent-catch risk. Good change.
- **Canary observations:** `api/router.js` continues to be the sole source of CORS + admin gating; `ADMIN_ONLY` now includes both `voice-clone` and `voice-mirror`, so the new route is covered. `lib/pool.js`, `lib/supabase.js`, `lib/voice.js`, `routes/evaluate.js` unchanged.
- **Week-over-week trend:** `.env.example` landed on 2026-04-18, closing the env-drift check for the first time. Expect this section to remain ✅ unless new env vars are added without doc updates.
- **Pattern drift worth watching:** three ADMIN_ONLY routes (`voice-mirror`, `voice-clone`, `assess`) omit per-handler auth while the other seven ADMIN_ONLY routes include it. If the router-level gate is ever loosened, these three would be exposed. Candidate for a one-line defense-in-depth backfill, but not urgent.
