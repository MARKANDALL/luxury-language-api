# Backend Nightly Health Scan — 2026-04-19

## Summary
0 pool drift | 0 silent catches | 0 missing auth gate | 0 CORS drift | 0 env var drift

**✅ All checks passed**

## Findings

### 1. Postgres pool drift
✅ Clean

All route files that touch Postgres import the shared singleton:
- `routes/admin-recent.js:2`
- `routes/attempt.js:4`
- `routes/convo-report.js:6`
- `routes/migrate.js:1`
- `routes/update-attempt.js:4`
- `routes/user-recent.js:5`

No `new Pool(...)` found outside `lib/pool.js`. The only `globalThis.__lux_pool` references outside the canary are in `test/attempt.contract.test.js:7-8`, which legitimately reset the singleton between tests.

### 2. Silent catches
✅ Clean

Every `catch` block inspected in non-canary files either logs via `console.error`/`console.warn` or returns a deliberate error response. Representative samples: `routes/voice-clone.js:55,113,155`, `routes/voice-mirror.js:65`, `routes/convo-turn.js:360`, `routes/update-attempt.js:44`, `routes/admin-recent.js:162`, `routes/alt-meaning.js:34,138`, `routes/tts.js:63,241`, `routes/tts/speak.js:41,79,98,101`, `routes/attempt.js:180`, `routes/admin-label-user.js:28,74`, `routes/migrate.js:48`, `routes/realtime-webrtc-session.js:153`, `routes/user-recent.js:52`, `routes/convo-report.js:184`, `routes/admin-user-stats.js:32,132`, `routes/pronunciation-gpt.js:55,104`, `routes/pronunciation-gpt/translate.js:26`, `routes/pronunciation-gpt/historySummary.js:115`.

### 3. Missing auth gate on paid routes
✅ Clean

All paid-API routes are gated centrally by the router's `ADMIN_ONLY` set (`api/router.js:247-265`), which rejects with 401 before dispatch:
- `tts` (Azure TTS) → `routes/tts.js`
- `pronunciation-gpt` (OpenAI) → `routes/pronunciation-gpt.js`
- `evaluate` (Azure Speech) → `routes/evaluate.js`
- `assess` (Azure Speech) → `routes/assess.js`
- `convo-turn` (OpenAI) → `routes/convo-turn.js`
- `convo-report` (OpenAI) → `routes/convo-report.js`
- `alt-meaning` (OpenAI) → `routes/alt-meaning.js`
- `realtime/webrtc/session` (OpenAI) → `routes/realtime-webrtc-session.js`
- `voice-clone` (ElevenLabs) → `routes/voice-clone.js`
- `voice-mirror` (ElevenLabs) → `routes/voice-mirror.js`

Most routes also carry an in-handler `x-admin-token` re-check (`routes/pronunciation-gpt.js:41-43`, `routes/alt-meaning.js:20-22`, `routes/convo-turn.js:303-305`, `routes/convo-report.js:117-119`, `routes/tts.js:39-43`, `routes/realtime-webrtc-session.js:50-53`) as defense-in-depth.

`voice-clone.js` and `voice-mirror.js` rely solely on the router-level gate — acceptable under the canonical "router-only" pattern but worth noting (see Notes).

### 4. CORS drift
✅ Clean

CORS is handled centrally in `api/router.js:200-244`: explicit origin allowlist from `CORS_ORIGINS` plus localhost and `lux-frontend*.vercel.app`, no wildcards. OPTIONS preflight returns 204 at `api/router.js:240-244`. `vercel.json` funnels every `/api/:route` through the router, so no route file can bypass it.

Two downstream handlers also return their own 204 on OPTIONS (`routes/voice-clone.js:8`, `routes/voice-mirror.js:8`) — redundant but harmless; the router intercepts OPTIONS before these run.

### 5. Env var drift
✅ Clean

Every `process.env.*` reference resolves to an entry in `.env.example` (either active or as a commented alias):

| Var | Location(s) | `.env.example` |
|-----|-------------|----------------|
| `ADMIN_TOKEN` | router, 8 routes | active |
| `AZURE_SPEECH_KEY` | evaluate, tts, assess, router | active |
| `AZURE_SPEECH_REGION` | evaluate, tts, assess, router | active |
| `AZURE_REGION` | evaluate, tts, assess | commented alias |
| `AZURE_TTS_KEY` | tts | commented alias |
| `OPENAI_API_KEY` | pronunciation-gpt, alt-meaning, convo-turn, convo-report, realtime-webrtc-session, router | active |
| `LUX_AI_QUICK_MODEL` / `DEEP_MODEL` / `CONVO_MODEL` / `REPORT_MODEL` / `TRANSLATE_MODEL` / `DEEP_REASONING_MODEL` / `DEEP_REASONING_EFFORT` / `MODEL` | various | all commented |
| `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` | lib/supabase, historySummary, router | active |
| `SUPABASE_SERVICE_ROLE`, `SUPABASE_SERVICE_ROLE_KEY_JWT`, `SUPABASE_SERVICE_KEY`, `SUPABASE_ANON_KEY`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` | lib/supabase, historySummary | commented aliases |
| `POSTGRES_URL` | lib/pool | active |
| `POSTGRES_CONNECTION`, `DATABASE_URL`, `PGSSLMODE` | lib/pool | commented aliases |
| `ELEVENLABS_API_KEY` | lib/voice | active |
| `CORS_ORIGINS` | router | active |
| `ENABLE_PROSODY` | assess | active |

No orphan entries in `.env.example` — every documented var is referenced somewhere in code.

## Notes

- **Canary shape is intact.** `api/router.js` still owns CORS and `ADMIN_ONLY` gating centrally (router.js:200-265); `lib/pool.js` remains the single `pg.Pool` owner. `vercel.json` continues to route `/api/:route` → `/api/router?route=:route`, so no route file can be reached without passing through the gate. This matches the post-Feb/March 2026 refactors (`3b804fa` pool singleton, `71a4429` CORS router-only fix).
- **Recent 24 h:** only one commit landed — `87807a9 fix(voice-mirror): log last_used_at update failures; add .env.example`. This added the `.env.example` file this scan relies on and also added the `.catch(err => console.warn(...))` on the fire-and-forget Supabase update at `routes/voice-mirror.js:57`. No regressions introduced.
- **Non-canary cosmetic:** `routes/assess.js:141,146` contain orphan `if (inputPath) {    }` empty-body blocks inside the `finally` cleanup, and the `outputPath` removal is duplicated at lines 138-139 and 143-144. Not one of the five tracked regressions, but a leftover from a prior hand-edit that Mark may want to tidy.
- **Borderline, not flagged:** `routes/pronunciation-gpt/runCoach.js:128` catches a JSON parse error without logging and falls through to `parseJsonWithRepair`. Treated as canonical `jsonrepair` fallback rather than a silent swallow — the outer `routes/pronunciation-gpt.js:104` catch will still log if repair also throws.
- **Defense-in-depth posture:** `voice-clone` and `voice-mirror` depend solely on the router gate. If the router-only pattern is ever relaxed, these two should be the first to grow their own `x-admin-token` check.
