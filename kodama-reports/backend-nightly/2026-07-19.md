# Backend Health Scan — 2026-07-19

## Summary
[0] pool drift | [1] silent catches | [5 INFO] missing auth gate | [0] CORS drift | [3] env var drift

## Top Priority
If you spend 15 minutes this week, document `GEMINI_API_KEY`, `LUX_IMAGE_PROVIDER`, and `FRONTEND_URL` in `.env.example` — `routes/convo-image.js` needs all three to run, but a fresh deploy has no way to know that.

## Findings

### 1. Postgres pool drift
✅ Clean — only `lib/pool.js` constructs `new Pool(...)`; every other reference is the singleton on `globalThis.__lux_pool` (test/attempt.contract.test.js:8 resets it, which is legitimate test isolation).

### 2. Silent catches
`routes/convo-image.js:26` — `fetchImageAsBase64` catches a fetch failure and `return null;` with no console/logger call and no comment marking the swallow as intentional. Callers do handle null, but the network error itself vanishes silently.

### 3. Missing auth gate on paid routes
✅ No HIGH — every paid route is either self-gated or in `api/router.js` `ADMIN_ONLY` (defense-in-depth check gates them at the router before dispatch).

INFO (router-gated only; no per-handler check — architectural debt, not a live hole):
- `routes/assess.js:17` — Azure Speech
- `routes/convo-image.js:44` — Gemini
- `routes/dictate.js:24` — Azure Speech
- `routes/voice-clone.js` — ElevenLabs
- `routes/voice-mirror.js` — ElevenLabs

### 4. CORS drift
✅ Clean — CORS is fully centralized in `api/router.js:241-281`. No route file overrides or wildcards `Access-Control-Allow-Origin`; the only other match in the tree is `scripts/hygiene-report.backend.mjs`'s own detection regex.

### 5. Env var drift
Referenced in code but missing from `.env.example`:
- `GEMINI_API_KEY` — `routes/convo-image.js:9,53`
- `LUX_IMAGE_PROVIDER` — `routes/convo-image.js:48` (selects Gemini vs. Luma)
- `FRONTEND_URL` — `routes/convo-image.js:40` (used to build character-portrait / video-still asset URLs)

Entries in `.env.example` referenced nowhere in code:
✅ None — every uncommented `.env.example` entry has a live reference.

## Legacy Backlog
- `routes/convo-image-luma.js:26` — silent catch (`return null;` on a fetch failure); file last touched 9 weeks ago.
- `routes/convo-image-luma.js:8` — `LUMA_IMAGE_MODEL` missing from `.env.example`.
- `routes/convo-image-luma.js:11` — `FRONTEND_URL` reference (also referenced from the fresh `convo-image.js` above; listed in main).
- `routes/convo-image-luma.js:50,64,109` — `LUMA_API_KEY` missing from `.env.example`.
- `scripts/hygiene-report.backend.mjs:186` — silent catch (`return;`) in the hygiene script itself; last touched 3 months ago.

## Notes
- Activity this week (5 commits, all inside scope): learner-model read route landed (#56) plus a 500-hardening follow-up (#57); coach-ask persona + word-info surface whitelist (#52); historySummary/word-info tap logging fixes (#53); pronunciation-gpt scrutiny remap + coach strictness (#55). Every one of these files has a clean per-handler `ADMIN_TOKEN` check.
- Canary observation (not a finding): `api/router.js`'s router-level `ADMIN_ONLY` set is now the sole security wall for `assess`, `dictate`, `convo-image`, `voice-clone`, and `voice-mirror`. That is intentional per the current pattern — but it does mean if any of those routes were ever mounted outside the router, they'd be wide open. Worth keeping in mind if the router is ever bypassed for local testing.
- Canary observation: `routes/evaluate.js` also has no per-handler admin check (relies on router gating). Same posture as the routes above; noted here rather than in Findings per the canary rule.
- The new `routes/learner-model.js` mirrors `word-history.js`'s belt-and-suspenders posture (per-handler admin gate + `sb = null` graceful degrade + top-level try/catch returning the empty portrait at 200) — clean pattern to reuse for future read-only routes.
- Week-over-week: no regressions in the pool/CORS/silent-catch categories. The three fresh env-var gaps all stem from `convo-image.js` — the file predates the `.env.example` cutoff (2026-04-18) and was never backfilled.
