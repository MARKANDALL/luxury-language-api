# Backend Architecture Audit — 2026 Week 18

## Summary
- Oversized route files: 1
- Dead/single-use lib helpers: 0
- Router registration issues: 0

## Top Priority
`routes/convo-turn.js` (509 lines) carries three unrelated responsibilities — prompt-table data, length-repair logic, and the HTTP handler — and is the only file that warrants a split.

## Findings

### 1. Oversized route files (>400 lines)

| File | Lines | Description | Splittable? |
|------|-------|-------------|-------------|
| `routes/convo-turn.js` | 509 | Conversation-turn handler: CEFR/tone/length prompt tables, length-repair helpers, system-prompt builder, OpenAI call, JSON response. | **Yes** — three distinct concerns: (a) static prompt-knob tables (`LEVEL_INSTRUCTIONS`, `TONE_INSTRUCTIONS`, `LENGTH_INSTRUCTIONS`), (b) length analysis + repair (`sentenceCount`, `wordCount`, `getLengthOutlierThresholds`, `isLengthOutlier`, `buildLengthRepairPrompt`, `maybeRepairAssistantLength`), and (c) the request handler. The prompt tables are pure data and the length-repair stack is self-contained — both could move to siblings (e.g. `routes/convo-turn/prompts.js`, `routes/convo-turn/lengthRepair.js`) leaving the handler ~200 lines. |

Stability exception not applicable: file has 20+ commits in the last 60 days (active churn around character-anchor and length-repair work).

### 2. Lib helper usage

| Helper | Routes importing | Status |
|--------|-----------------|--------|
| `lib/pool.js` | 6 | Healthy — top canary |
| `lib/supabase.js` | 5 | Healthy — top canary |
| `lib/voice.js` | 2 | Healthy |

No 0-import or 1-import lib helpers. All three lib files are shared by ≥2 routes; nothing flagged.

**Top 3 most-imported lib helpers (true canaries — extra care):**
1. `lib/pool.js` — used by `migrate`, `update-attempt`, `convo-report`, `attempt`, `admin-recent`, `user-recent` (6 routes).
2. `lib/supabase.js` — used by `voice-mirror`, `admin-user-stats`, `admin-label-user`, `voice-clone`, `pronunciation-gpt/historySummary` (5 routes).
3. `lib/voice.js` — used by `voice-mirror`, `voice-clone` (2 routes).

### 3. Router health (`api/router.js`)

- **Routes registered in `ROUTES`:** 19 (including `ping`/`health` aliases, 4 lazy-loaded handlers, 14 statically-imported handlers).
- **Dead imports (imported but not in `ROUTES`):** None. All 14 static imports are referenced in `ROUTES`.
- **Broken registrations (`ROUTES` entries pointing at undefined symbols):** None. Every key resolves to a defined symbol.
- **Lazy-loaded targets that don't exist on disk:** None.
  - `routes/evaluate.js` ✓ (115 lines)
  - `routes/assess.js` ✓ (145 lines)
  - `routes/tts.js` ✓ (242 lines)
  - `routes/pronunciation-gpt.js` ✓ (109 lines)

Router is internally consistent: imports, registrations, and lazy targets all line up.

## Notes

- **Canary observations.** `routes/evaluate.js`, `lib/pool.js`, `lib/supabase.js`, `lib/voice.js`, `api/router.js`, `vercel.json`, `package.json` are all in their expected shape. The router's lazy-load wrapper (`lazyRoute`) continues to isolate the four heaviest/most-fragile routes (`evaluate`, `assess`, `tts`, `pronunciation-gpt`) — a healthy pattern; recommend keeping it as new heavy routes are added.
- **Trends.** Recent 14-day churn touched 9 route files plus `api/router.js`. Most growth concentrated in `convo-turn.js` (character-anchor v2/v3, length repair, graceful hard-cutoff handling) — this file is becoming the de-facto conversation engine and is the strongest candidate for modularization next time it's edited substantively.
- **Stable patterns.** `routes/pronunciation-gpt/` already demonstrates the split-by-concern pattern (8 small files: `prompt`, `personas`, `scoring`, `runCoach`, `historySummary`, `translate`, `azureExtract`, `json`). Same pattern would fit `convo-turn` cleanly.
- **No prior audits found** in `kodama-reports/backend-weekly/`. This is the baseline report; future audits will compare against it.
