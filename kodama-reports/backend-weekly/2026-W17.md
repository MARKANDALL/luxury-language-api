# Backend Architecture Audit — 2026 Week 17

## Summary
- Oversized route files: 0
- Dead/single-use lib helpers: 0
- Router registration issues: 0

## Top Priority
None — architecture is stable.

## Findings

### 1. Oversized route files (>400 lines)
No route file under `routes/` exceeds 400 lines. Longest is `routes/convo-turn.js` at 363 lines. Next-largest: `routes/tts.js` (245), `routes/realtime-webrtc-session.js` (216), `routes/convo-report.js` (187), `routes/attempt.js` (185). Nothing to split.

### 2. Lib helper usage
The `lib/` directory holds three helpers. Import counts across `routes/` (including subdirectories):

| Helper | Importing route files | Count |
|---|---|---|
| `lib/pool.js` | admin-recent, attempt, convo-report, migrate, update-attempt, user-recent | 6 |
| `lib/supabase.js` | admin-label-user, admin-user-stats, voice-clone, voice-mirror, pronunciation-gpt/historySummary | 5 |
| `lib/voice.js` | voice-clone, voice-mirror | 2 |

- **Top 3 most-imported helpers (canaries):** `lib/pool.js` (6), `lib/supabase.js` (5), `lib/voice.js` (2). All three are canary files per the audit brief — treat with care.
- **Dead code candidates (0 routes):** none.
- **Single-use helpers (1 route):** none. `lib/voice.js` has two importers (voice-clone, voice-mirror); its Azure TTS surface is specifically shared between those two, so the dual-use pattern is intentional, not accidental.

### 3. Router health
`api/router.js` eagerly imports 13 handlers and lazy-loads 4 more. `ROUTES` registers 19 entries (17 route handlers + `ping` + `health` alias).

- **Dead imports (imported but not in ROUTES):** none. All 13 eager imports appear in the `ROUTES` const.
- **Broken registrations (ROUTES entry references an undefined symbol):** none. Every value resolves (`ping` is defined inline; all others are imported or built by `lazyRoute`).
- **Lazy route targets missing on disk:** none. `../routes/evaluate.js`, `../routes/assess.js`, `../routes/tts.js`, and `../routes/pronunciation-gpt.js` all exist.

Router gate `ADMIN_ONLY` covers 10 routes; the remaining registered routes (attempt, update-attempt, user-recent, admin-*, migrate, ping/health) are intentionally not admin-gated. No drift observed.

## Notes
- Only one substantive backend change in the 14-day window: `routes/voice-mirror.js` (`87807a9` — log `last_used_at` update failures, add `.env.example`). Scope was localised to that route plus a dotenv example; no structural impact.
- Canary files (`api/router.js`, `lib/pool.js`, `lib/supabase.js`, `lib/voice.js`, `vercel.json`, `package.json`, `routes/evaluate.js`) all untouched this window — good stability signal.
- `routes/` still contains `.GOLD` snapshot siblings for several files (assess, attempt, convo-report, convo-turn, pronunciation-gpt, admin-recent, migrate, update-attempt, user-recent). These are correctly excluded by the audit filter and do not ship (router imports only `.js`). They do clutter the directory listing; not a finding, but worth a future cleanup pass if they ever drift from their counterparts.
- `routes/pronunciation-gpt/` and `routes/tts/` subfolders hold small single-purpose helpers for their parent route files. Pattern is consistent and healthy — no oversized files and clear responsibility boundaries.
- Trend vs. prior audits: with only one small commit in the window, no movement in line counts, no new lib helpers, and no router churn. Architecture is stable.
