# Backend Architecture Audit — 2026 Week 16

## Summary
- Oversized route files: 0
- Dead/single-use lib helpers: 0
- Router registration issues: 0

## Top Priority
None — architecture is stable.

## Findings

### 1. Oversized route files (>400 lines)
No route files exceed the 400-line threshold. Largest files (for situational awareness only):

| File | Lines |
| --- | --- |
| routes/convo-turn.js | 363 |
| routes/tts.js | 245 |
| routes/realtime-webrtc-session.js | 216 |
| routes/convo-report.js | 187 |
| routes/attempt.js | 185 |

`convo-turn.js` is the closest to the threshold and worth keeping an eye on in future audits, but at 363 lines it does not warrant a split today.

### 2. Lib helper usage
All three helpers in `lib/` are actively used by multiple routes. No dead or single-use candidates.

| Helper | Importing routes | Count |
| --- | --- | --- |
| lib/pool.js | convo-report, attempt, migrate, admin-recent, user-recent, update-attempt | 6 |
| lib/supabase.js | voice-clone, admin-label-user, admin-user-stats, voice-mirror, pronunciation-gpt/historySummary | 5 |
| lib/voice.js | voice-clone, voice-mirror | 2 |

Top 3 most-imported (all canaries — treat with extra care):
1. `lib/pool.js` (6 routes) — Postgres pool, highest blast radius.
2. `lib/supabase.js` (5 routes) — Supabase admin client; auth/admin surface.
3. `lib/voice.js` (2 routes) — ElevenLabs voice helpers; both routes are voice-cloning related, so this helper is appropriately scoped rather than under-used.

### 3. Router health (`api/router.js`)
- **Registered routes in `ROUTES`:** 19 entries (`ping`, `health` alias, `admin-label-user`, `admin-recent`, `admin-user-stats`, `alt-meaning`, `assess`, `attempt`, `convo-report`, `convo-turn`, `evaluate`, `migrate`, `pronunciation-gpt`, `realtime/webrtc/session`, `tts`, `update-attempt`, `user-recent`, `voice-clone`, `voice-mirror`).
- **Dead imports:** none. Every static `import` at the top of `api/router.js` is wired into `ROUTES`.
- **Broken registrations:** none. Every `ROUTES` value references an in-scope symbol (static import, lazy wrapper, or local `ping`).
- **Missing lazy targets:** none. All four lazy targets exist on disk:
  - `routes/evaluate.js` ✓
  - `routes/assess.js` ✓
  - `routes/tts.js` ✓
  - `routes/pronunciation-gpt.js` ✓

## Notes
- **Activity window (last 14 days):** only `routes/voice-mirror.js` shows substantive changes. Canary files (`api/router.js`, `lib/pool.js`, `lib/supabase.js`, `lib/voice.js`, `vercel.json`, `package.json`, `routes/evaluate.js`) were untouched this window — stable baseline.
- **Canary stewardship:** `lib/pool.js` and `lib/supabase.js` remain the two highest-fan-in helpers; any change to their public surface still carries the largest blast radius in the backend.
- **Pattern watch:** `routes/convo-turn.js` (363 lines) continues to grow slowly relative to the rest of `routes/`. Not a finding yet, but flag if it crosses 400 in a future audit.
- **ADMIN_ONLY gating:** the router-level admin allowlist in `api/router.js` currently covers 10 of the 17 non-trivial routes. Nothing new there this cycle; noted for continuity.
- **First audit in this series:** no prior `kodama-reports/backend-weekly/` entries exist in git history, so there is no trend baseline yet. Subsequent audits will have this report to compare against.
