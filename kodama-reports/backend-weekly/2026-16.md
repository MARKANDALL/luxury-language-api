# Backend Architecture Audit — 2026 Week 16

## Summary
- Oversized route files: 0
- Dead/single-use lib helpers: 0
- Router registration issues: 0

## Top Priority
None — architecture is stable.

## Findings

### 1. Oversized route files (>400 lines)
None. Largest route is `routes/convo-turn.js` at 363 lines, comfortably under the threshold. Full distribution (non-GOLD):

| Lines | File |
| ----- | ---- |
| 363 | routes/convo-turn.js |
| 245 | routes/tts.js |
| 216 | routes/realtime-webrtc-session.js |
| 187 | routes/convo-report.js |
| 185 | routes/attempt.js |
| 168 | routes/admin-recent.js |
| 158 | routes/voice-clone.js |
| 147 | routes/assess.js |
| 142 | routes/alt-meaning.js |
| 136 | routes/admin-user-stats.js |
| 130 | routes/evaluate.js |
| 109 | routes/pronunciation-gpt.js |
| ≤78 | remaining 5 routes |

### 2. Lib helper usage
No dead helpers (0 imports) and no single-use helpers (1 import). Import counts across non-GOLD route files:

| lib helper | route imports |
| ---------- | ------------- |
| lib/pool.js | 6 (attempt, user-recent, migrate, convo-report, update-attempt, admin-recent) |
| lib/supabase.js | 5 (admin-user-stats, admin-label-user, voice-clone, voice-mirror, pronunciation-gpt/historySummary) |
| lib/voice.js | 2 (voice-clone, voice-mirror) |

Top 3 most-imported lib helpers: `pool.js`, `supabase.js`, `voice.js` — these match the declared canaries and continue to be load-bearing. Treat any change with extra care.

### 3. Router health
`api/router.js` registers **19** entries in `ROUTES` (17 distinct handlers; `ping`/`health` share the inline `ping` function). All clean:

- **Static imports → ROUTES:** all 13 imported symbols (`adminLabelUser`, `adminRecent`, `adminUserStats`, `attempt`, `altMeaning`, `convoReport`, `convoTurn`, `migrate`, `realtimeWebrtcSession`, `updateAttempt`, `userRecent`, `voiceClone`, `voiceMirror`) are registered. No dead imports.
- **ROUTES → symbols:** every value references a defined symbol (static import, inline `ping`, or lazy wrapper). No broken registrations.
- **Lazy targets:** `routes/evaluate.js`, `routes/assess.js`, `routes/tts.js`, `routes/pronunciation-gpt.js` all exist on disk. No missing targets.

## Notes

- **Activity gate:** Only `routes/voice-mirror.js` changed in the last 14 days (commit `87807a9` — improved failure logging for `last_used_at` updates). Scope was narrow and does not alter architecture.
- **Canary observations:** `api/router.js`, `lib/pool.js`, `lib/supabase.js`, `lib/voice.js`, `vercel.json`, `package.json`, `routes/evaluate.js` all untouched this cycle. Informational only.
- **GOLD snapshots:** The `routes/` directory contains 12 `*.GOLD*` snapshot files alongside live routes. These are intentionally excluded from all counts via the activity-gate pattern and line-count scan (only `*.js` without GOLD/ARCHIVE suffix). No action recommended — this is an established convention.
- **Trend since last audit:** No new routes added, no routes removed, no lib helpers added or removed, no registrations changed. Architecture surface is identical to the prior audit aside from the voice-mirror logging tweak.
