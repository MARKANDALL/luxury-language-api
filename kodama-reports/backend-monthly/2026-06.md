# Backend Monthly Hygiene Sweep — June 2026

Sweep date: 2026-06-01 · Branch: `claude/admiring-cori-A90FM` · Prior run: `kodama-reports/backend-monthly/2026-05.md` (PR #21)

## Summary
- .GOLD files: **11** total, **0** older than 60 days (delta from prior: **no change**, 11 → 11)
- Unused dependency candidates: **1** (`node-wav`, unchanged from prior)
- Stale branches: **5** (informational; 1 newly stale this month)
- Env var drift: **5** missing from `.env.example`, **0** unused in code
- README mismatches: **0**

## Top priority
The five undocumented env vars (`FRONTEND_URL`, `GEMINI_API_KEY`, `LUMA_API_KEY`, `LUMA_IMAGE_MODEL`, `LUX_IMAGE_PROVIDER`) are the only actionable item. They were introduced with the image-generation routes and have been flagged by every weekly health scan since 2026-05-03 (PRs #24, #28, #30, #33). Report-only PR — nothing here is being changed.

---

## Findings

### 1. `.GOLD` backup cleanup candidates

Filesystem enumeration (`find . -name '*.GOLD*'`, excluding `node_modules/`, `.vercel/`, `_ARCHIVE/`). `git ls-files` would miss these — `.gitignore` lists `*.GOLD*`.

| Path | Size (KB) | Modified | Age (days) |
|---|---:|---|---:|
| `admin/admin-label-user.js.GOLD` | 2.2 | 2026-05-30 | 2 |
| `index.html.GOLD` | 15.8 | 2026-05-30 | 2 |
| `routes/admin-recent.js.GOLD` | 4.7 | 2026-05-30 | 2 |
| `routes/assess.js.GOLD` | 4.9 | 2026-05-30 | 2 |
| `routes/attempt.js.GOLD` | 7.3 | 2026-05-30 | 2 |
| `routes/convo-report.js.GOLD` | 5.8 | 2026-05-30 | 2 |
| `routes/convo-turn.js.GOLD` | 13.5 | 2026-05-30 | 2 |
| `routes/migrate.js.GOLD` | 2.1 | 2026-05-30 | 2 |
| `routes/pronunciation-gpt.js.GOLD` | 18.3 | 2026-05-30 | 2 |
| `routes/update-attempt.js.GOLD` | 1.9 | 2026-05-30 | 2 |
| `routes/user-recent.js.GOLD` | 1.9 | 2026-05-30 | 2 |

- Total: 11 files, 78.4 KB.
- **0 are older than 60 days** by filesystem mtime — no strong cleanup candidates this cycle.
- Caveat: mtime reflects the ephemeral container clone date (2026-05-30), not original creation. The prior monthly report (May 2026, PR #21) attested these were created on 2026-04-26, so the real age is ~36 days. Still under the 60-day threshold; flag deferred.
- Count delta vs prior monthly run: **no change** (11 → 11). The 2026-04-15 task brief referenced `.GOLD.2` files (`attempt.js`, `migrate.js`, `pronunciation-gpt.js`, `update-attempt.js`, `user-recent.js`) — all five now exist as plain `.GOLD` instead; no `.GOLD.<number>` files remain on disk.
- ✅ Nothing to clean up.

### 2. Unused dependency candidates

Read `package.json`, then grepped `*.js` / `*.mjs` / `*.cjs` / `*.ts` (excluding `node_modules`, `.vercel`, `_ARCHIVE`, `.git`) for `require`/`import`/`from` of each declared package.

| Package | Code references | Status |
|---|---:|---|
| `@ffmpeg-installer/ffmpeg` | 2 (`routes/assess.js:5`, `test/assess.contract.test.js`) | ✅ used |
| `@google/genai` | 1 (`routes/convo-image.js:5`) | ✅ used |
| `@supabase/supabase-js` | 1 (`lib/supabase.js:1`) | ✅ used |
| `fluent-ffmpeg` | 2 (`routes/assess.js:4`, `test/assess.contract.test.js`) | ✅ used |
| `formidable` | 2 | ✅ used |
| `jsonrepair` | 2 | ✅ used |
| `microsoft-cognitiveservices-speech-sdk` | 2 | ✅ used |
| `node-wav` | **0** | ❌ **unused candidate** |
| `openai` | 4 | ✅ used |
| `pg` | 2 (`lib/pool.js:1`, `test/attempt.contract.test.js`) | ✅ used |
| `supertest` | 3 | ✅ used (test) |
| `vitest` | 4 | ✅ used (test + scripts) |

- **`node-wav`** declared at `package.json:22` (`^0.0.2`). No `require`, `import`, or dynamic reference anywhere under `routes/`, `lib/`, `api/`, `admin/`, `scripts/`, or `test/`. Not present in `vercel.json` or `package.json` scripts.
  - Self-check: regrep against `vercel.json` and the `scripts` block — neither contains it.
  - README mentions `node-wav` in the audio stack description (line 36); removing the dep would warrant a one-line README edit.
  - Status carries forward from May 2026 monthly report (PR #21) — same finding, no change.
- All other dependencies confirmed in use. None of the exclusion classes (`@types/*`, `@vercel/*`) appear in this `package.json`.

### 3. Stale branches (informational)

Threshold: no commits in 60+ days (before 2026-04-02). Excludes `main` and any branch referenced in an open PR. Cross-checked against the 21 open PRs in this repo.

| Branch | Last commit | Age (days) |
|---|---|---:|
| `chore/single-api-router` | 2025-12-28 | 155 |
| `claude/audit-duplicate-code-Kw0Z0` | 2026-02-26 | 95 |
| `claude/refactor-luxconvo-scenarios-xk7lC` | 2026-02-26 | 95 |
| `claude/system-health-bill-of-rights-B0H9o` | 2026-03-01 | 92 |
| `api-audit-cleanup-2026-02-28` | 2026-03-29 | 64 |

Delta vs May 2026 monthly: +1 newly stale (`api-audit-cleanup-2026-02-28` crossed the 60d line this cycle). The other four are carry-forward.

### 4. Env var drift

Extracted `process.env.X` from all `*.js`/`*.mjs`/`*.cjs` under the repo (excluding `node_modules`, `.vercel`, `_ARCHIVE`, `.git`) and compared against `.env.example`. Commented aliases in the example file count as documented.

**Referenced in code, missing from `.env.example`** (5):

| Var | First reference | Notes |
|---|---|---|
| `FRONTEND_URL` | `routes/convo-image.js:40`, `routes/convo-image-luma.js:11` | Defaults to `http://localhost:3000`; production must override |
| `GEMINI_API_KEY` | `routes/convo-image.js:9`, `:53` | Hard requirement for Gemini image generation; route 500s without it |
| `LUMA_API_KEY` | `routes/convo-image-luma.js:50`, `:64`, `:109` | Required when the Luma provider is selected |
| `LUMA_IMAGE_MODEL` | `routes/convo-image-luma.js:8` | Optional; defaults to `uni-1-max` |
| `LUX_IMAGE_PROVIDER` | `routes/convo-image.js:48` | Toggles Gemini ↔ Luma routing |

**In `.env.example`, unreferenced in code**: 0. Every active and commented-alias entry in `.env.example` maps to a `process.env.*` site.

This drift has been flagged in four consecutive weekly health scans (PRs #24, #28, #30, #33) and remains unaddressed. The monthly sweep records it but doesn't escalate beyond what the nightly tracker (#8) is already doing.

### 5. README accuracy

`README.md` Development section (lines 121–127):

```bash
npm install
npm test              # run Vitest contract tests
npm run test:watch    # watch mode
npm run hygiene       # backend hygiene report
```

| Command | Resolution | Status |
|---|---|---|
| `npm install` | npm built-in | ✅ |
| `npm test` | `package.json` scripts: `"test": "vitest run"` | ✅ |
| `npm run test:watch` | `package.json` scripts: `"test:watch": "vitest"` | ✅ |
| `npm run hygiene` | `package.json` scripts: `"hygiene": "node scripts/hygiene-report.backend.mjs"`; `scripts/hygiene-report.backend.mjs` exists on disk | ✅ |

✅ Nothing to clean up — a contributor following the README would not error.

---

## Notes

- The `.GOLD` inventory has been stable for two consecutive monthly cycles. If the intent is for `.GOLD` files to remain as in-place backups, the 60-day flag will start triggering around the next sweep (2026-07) for files that survive that long. Worth deciding whether they're meant to be transient (delete after a release) or durable (keep as long-term references).
- The 2026-04-15 task brief's reference list (`*.GOLD.2`) no longer matches reality — naming converged on plain `.GOLD` last month. Suggest updating the brief's known-set on the next revision.
- `node-wav` has now been a confirmed unused candidate for two months running. Safe to remove with `npm uninstall node-wav` + a one-line README tweak whenever convenient, but this is a report-only sweep so no action is taken here.
- Env var drift is a known regression from the image-generation rollout. The monthly sweep defers to the nightly tracker (#8) for follow-through.
- The newest stale-branch addition (`api-audit-cleanup-2026-02-28`) is the only branch movement vs May; nothing was deleted, nothing was un-staled.
