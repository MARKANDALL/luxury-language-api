# Backend Monthly Hygiene Sweep — May 2026

## Summary
- .GOLD files: 11 total, 0 older than 60 days (delta from prior: first run)
- Unused dependency candidates: 1 (`node-wav`)
- Stale branches: 4
- Env var drift: 0 missing from .env.example, 0 unused in code
- README mismatches: 0

## Findings

### 1. .GOLD backup cleanup candidates

Filesystem enumeration (`find . -name '*.GOLD*'`, excluding `node_modules/`, `.vercel/`, underscore-prefixed dirs). All `.GOLD` files are gitignored — git-based scans miss them, so this list is from disk only.

| Path | Size (KB) | Last modified | Age (days) |
|---|---|---|---|
| `index.html.GOLD` | 15.8 | 2026-04-26 | 5 |
| `routes/pronunciation-gpt.js.GOLD` | 18.3 | 2026-04-26 | 5 |
| `routes/migrate.js.GOLD` | 2.1 | 2026-04-26 | 5 |
| `routes/convo-report.js.GOLD` | 5.8 | 2026-04-26 | 5 |
| `routes/user-recent.js.GOLD` | 1.9 | 2026-04-26 | 5 |
| `routes/attempt.js.GOLD` | 7.2 | 2026-04-26 | 5 |
| `routes/update-attempt.js.GOLD` | 2.1 | 2026-04-26 | 5 |
| `routes/assess.js.GOLD` | 4.9 | 2026-04-26 | 5 |
| `routes/admin-recent.js.GOLD` | 4.7 | 2026-04-26 | 5 |
| `routes/convo-turn.js.GOLD` | 13.5 | 2026-04-26 | 5 |
| `admin/admin-label-user.js.GOLD` | 2.2 | 2026-04-26 | 5 |

**Cleanup candidates (>60 days):** none.

**Delta vs prior run:** first monthly run — no prior `kodama-reports/backend-monthly/` report exists. The 2026-04-15 task brief noted 5 known `.GOLD.2` files (`routes/attempt.js`, `routes/migrate.js`, `routes/pronunciation-gpt.js`, `routes/update-attempt.js`, `routes/user-recent.js`); none of those `.GOLD.2` files exist on disk now. The current set of 11 `.GOLD` files (no numeric suffix) was created on 2026-04-26, so the naming convention has shifted from `.GOLD.<n>` to plain `.GOLD`. Informational only.

✅ Nothing to clean up.

### 2. Unused dependency candidates

Per-package grep (covers static `import`/`require` and `await import(...)`). Skipped `@types/*` and `@vercel/*` per rules; cross-checked `vercel.json` and `package.json` scripts.

| Package | Static refs | Dynamic-import refs | Verdict |
|---|---|---|---|
| `@ffmpeg-installer/ffmpeg` | 1 | — | used |
| `@supabase/supabase-js` | 1 | — | used |
| `fluent-ffmpeg` | 1 | — | used |
| `formidable` | 2 | — | used |
| `jsonrepair` | 0 | `routes/pronunciation-gpt.js`, `routes/alt-meaning.js` | used (dynamic) |
| `microsoft-cognitiveservices-speech-sdk` | 1 | — | used |
| `node-wav` | 0 | 0 | **unused candidate** |
| `openai` | 0 | `routes/pronunciation-gpt.js`, `routes/convo-report.js`, `routes/convo-turn.js`, `routes/alt-meaning.js` | used (dynamic) |
| `pg` | 1 | — | used |
| `supertest` (dev) | 3 | — | used |
| `vitest` (dev) | 4 | — | used (and in `test`/`test:watch` scripts) |

**Candidate: `node-wav`** — declared at `package.json:22`. Zero references across the repo (`*.js`, `*.mjs`, `vercel.json`, scripts). Self-check: re-grepped for `node-wav`, `nodeWav`, `node_wav` and against `vercel.json` and `package.json` scripts — no hits. README mentions it ("…+ node-wav for audio handling") but no code imports it. Reportable as a removal candidate; verify before removing in case it's planned for an in-flight feature.

### 3. Stale branches (informational)

Cutoff: last commit older than 2026-03-02 (60 days from 2026-05-01). Excludes `main` and any branch attached to an open PR.

| Branch | Last commit | Age (days) |
|---|---|---|
| `origin/chore/single-api-router` | 2025-12-28 | 124 |
| `origin/claude/refactor-luxconvo-scenarios-xk7lC` | 2026-02-26 | 64 |
| `origin/claude/audit-duplicate-code-Kw0Z0` | 2026-02-26 | 64 |
| `origin/claude/system-health-bill-of-rights-B0H9o` | 2026-03-01 | 61 |

Cross-checked against the 9 currently-open PRs (#12–#20); none of the four branches above is attached to an open PR.

### 4. Env var drift

Compared `process.env.X` references in `.js`/`.mjs` against `.env.example` (active lines and commented aliases both count as "documented").

- Code references found: 29 unique vars.
- `.env.example` documents all 29 (some as commented aliases — e.g. `AZURE_REGION`, `AZURE_TTS_KEY`, `POSTGRES_CONNECTION`, `DATABASE_URL`, `PGSSLMODE`, the `LUX_AI_*` model overrides, and the Supabase aliases).
- `.env.example` lists no vars unused in code.

✅ Nothing to clean up.

### 5. README accuracy

`README.md` "Development" section (lines 112–119):

```bash
npm install
npm test              # run Vitest contract tests
npm run test:watch    # watch mode
npm run hygiene       # backend hygiene report
```

| Command | Resolves to | Status |
|---|---|---|
| `npm install` | standard npm | ✅ |
| `npm test` | `package.json:12` → `vitest run` | ✅ |
| `npm run test:watch` | `package.json:13` → `vitest` | ✅ |
| `npm run hygiene` | `package.json:11` → `node scripts/hygiene-report.backend.mjs` (file exists) | ✅ |

✅ Nothing to clean up.

## Notes

- All 11 `.GOLD` files were created in a single sweep on 2026-04-26 (matching commit activity in the active branch around that date) and are still well within the 60-day flagging window.
- The naming convention shifted from `.GOLD.<n>` to plain `.GOLD` since the prior tracker note (2026-04-15). Worth confirming with Mark whether this is intentional, since older `.GOLD.<n>` versions were apparently rotated out.
- `node-wav` is the only true unused-dependency candidate. The README still describes it as part of the audio stack — if removed, also drop the README mention. No action taken; report-only.
- Stale branch `chore/single-api-router` (2025-12-28) predates the canonical single-router refactor — likely safe to delete after a quick check that its commits all landed elsewhere, but not flagged for removal here.
