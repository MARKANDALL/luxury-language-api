# Backend Monthly Hygiene Sweep — April 2026

Run date: 2026-04-16
Branch: `claude/eloquent-ride-atSlr`
Scope excluded: `_ARCHIVE/`, `node_modules/`, `.vercel/`, any underscore-prefixed folder.

## Summary
- .GOLD files: 16 total, 0 older than 60 days (first run — no prior report for delta)
- Unused dependency candidates: 1 (`gpt-tokenizer`)
- Stale branches: 0
- Env var drift: `.env.example` does not exist (report-only; not auto-created)
- README mismatches: 0

## Findings

### 1. `.GOLD` backup cleanup candidates

Enumerated via filesystem (`find . -name '*.GOLD*'`), not git, per the detection rule. Note: all on-disk mtimes are 2026-04-16 21:02 — a clone artifact (identical to the second across every file), so mtime is not a meaningful age signal here. Ages below are computed from `git log -1 --format='%ai' -- <path>` (the last content commit), which reflects actual last edit.

As of 2026-04-16, 16 `.GOLD*` files present. None exceed the 60-day cleanup threshold. Oldest is 56 days.

| Path | Size | Last commit | Age (days) |
|---|---|---|---|
| `index.html.GOLD` | 15.8 KB | 2026-02-19 | 56 |
| `routes/pronunciation-gpt.js.GOLD` | 18.3 KB | 2026-02-25 | 50 |
| `routes/admin-recent.js.GOLD` | 4.7 KB | 2026-02-28 | 47 |
| `routes/assess.js.GOLD` | 4.9 KB | 2026-02-28 | 47 |
| `routes/attempt.js.GOLD` | 7.2 KB | 2026-02-28 | 47 |
| `routes/convo-report.js.GOLD` | 5.8 KB | 2026-02-28 | 47 |
| `routes/convo-turn.js.GOLD` | 13.5 KB | 2026-02-28 | 47 |
| `routes/migrate.js.GOLD` | 2.1 KB | 2026-02-28 | 47 |
| `routes/update-attempt.js.GOLD` | 2.1 KB | 2026-02-28 | 47 |
| `routes/user-recent.js.GOLD` | 1.9 KB | 2026-02-28 | 47 |
| `admin/admin-label-user.js.GOLD` | 2.2 KB | 2026-02-28 | 47 |
| `routes/attempt.js.GOLD.2` | 6.9 KB | 2026-03-01 | 46 |
| `routes/migrate.js.GOLD.2` | 1.8 KB | 2026-03-01 | 46 |
| `routes/pronunciation-gpt.js.GOLD.2` | 3.8 KB | 2026-03-01 | 46 |
| `routes/update-attempt.js.GOLD.2` | 1.8 KB | 2026-03-01 | 46 |
| `routes/user-recent.js.GOLD.2` | 1.6 KB | 2026-03-01 | 46 |

`.GOLD.2` files currently on disk (5): `attempt.js`, `migrate.js`, `pronunciation-gpt.js`, `update-attempt.js`, `user-recent.js`. This exactly matches the 2026-04-15 known set; delta vs baseline = 0. No prior monthly report in `kodama-reports/backend-monthly/`, so this is the first run and no month-over-month delta exists yet.

Self-check: Filesystem enumeration was used (`find`). `git ls-files` was only used as a cross-check — and noted that in this repo the `.GOLD` files were committed before the `*.GOLD` ignore rule was added, so they are still tracked. Both methods returned the same 16 paths.

Recommendation: no action this month. Revisit next month — `index.html.GOLD` will cross the 60-day line on 2026-04-20.

### 2. Unused dependency candidates

Checked every entry in `package.json` `dependencies` and `devDependencies` against imports in `*.{js,mjs,cjs,ts}`, `vercel.json`, and `package.json` scripts. No `@types/*` or `@vercel/*` entries exist, so those exclusions did not apply.

| Package | Evidence | Verdict |
|---|---|---|
| `@ffmpeg-installer/ffmpeg` | imported in `routes/assess.js` | used |
| `@supabase/supabase-js` | imported in `lib/supabase.js` | used |
| `fluent-ffmpeg` | imported in `routes/assess.js` | used |
| `formidable` | imported in `routes/assess.js`, `routes/evaluate.js` | used |
| `gpt-tokenizer` | no `import`/`require` anywhere; not in `vercel.json` or scripts; only mentioned in `README.md:60` prose | **unused candidate** |
| `jsonrepair` | imported in `routes/alt-meaning.js`, `routes/pronunciation-gpt.js`, `routes/pronunciation-gpt/json.js`, `routes/pronunciation-gpt/runCoach.js` | used |
| `microsoft-cognitiveservices-speech-sdk` | imported in `routes/evaluate.js`, `routes/tts/speak.js` | used |
| `node-wav` | imported in `routes/evaluate.js:6` | used |
| `openai` | dynamic `import("openai")` in `routes/pronunciation-gpt.js:51`, `routes/convo-turn.js:314`, `routes/alt-meaning.js:30`, `routes/convo-report.js:68` | used |
| `pg` | imported in `lib/pool.js:2` | used |
| `supertest` (dev) | imported in `test/assess.contract.test.js`, `test/attempt.contract.test.js`, `test/router.contract.test.js` | used |
| `vitest` (dev) | referenced in `package.json` scripts `test` and `test:watch` | used |

Self-check for `gpt-tokenizer`: re-searched for `tokenizer`, `tiktoken`, and `gpt.tokenizer` across source — zero matches. Not present in `vercel.json` or any `package.json` script. README prose mention is marketing copy, not executable.

Recommendation: candidate for removal, but confirm with repo owner first — the README claim "jsonrepair and gpt-tokenizer for structured model output" suggests it may have been planned/removed without updating docs, or may be intended for a near-term feature. Report-only per rules; no action taken.

### 3. Stale branches (informational)

Remote refs and last-commit timestamps:

| Branch | Last commit | Age |
|---|---|---|
| `origin/main` | 2026-04-16 16:51 | today |
| `origin/claude/eloquent-ride-atSlr` | 2026-04-16 16:51 | today |

No branches older than 60 days. `main` is excluded by rule regardless. Nothing to flag.

### 4. Env var drift

`.env.example` does not exist at the repo root. Cannot compute drift.

For reference, `process.env.*` names referenced in source (67 reads across 19 files): `ADMIN_TOKEN`, `AZURE_SPEECH_KEY`, `AZURE_SPEECH_REGION`, `AZURE_REGION`, `AZURE_TTS_KEY`, `OPENAI_API_KEY`, `ELEVENLABS_API_KEY`, `SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_SERVICE_ROLE_KEY_JWT`, `SUPABASE_SERVICE_KEY`, `SUPABASE_ANON_KEY`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `CORS_ORIGINS`, `ENABLE_PROSODY`, `LUX_AI_CONVO_MODEL`, `LUX_AI_QUICK_MODEL`, `LUX_AI_REPORT_MODEL`, `LUX_AI_MODEL`, `LUX_AI_DEEP_MODEL`, `LUX_AI_DEEP_REASONING_MODEL`, `LUX_AI_DEEP_REASONING_EFFORT`, `LUX_AI_TRANSLATE_MODEL`, `POSTGRES_URL`, `POSTGRES_CONNECTION`, `DATABASE_URL`, `PGSSLMODE`.

Recommendation: add a `.env.example` to the repo root so drift can be measured in future sweeps. Not auto-created here (report-only rule).

### 5. README accuracy

`README.md` "Development" section (lines 114–119) lists four commands. All valid:

| Command | Status |
|---|---|
| `npm install` | standalone, valid |
| `npm test` | present in `package.json` scripts (`vitest run`) |
| `npm run test:watch` | present in `package.json` scripts (`vitest`) |
| `npm run hygiene` | present in `package.json` scripts (`node scripts/hygiene-report.backend.mjs`) |

✅ Nothing to clean up.

## Notes

- The `*.GOLD` files are listed in `.gitignore` but remain tracked — they pre-date the ignore rule. A future cleanup could both remove the stale backups and drop them from the index so the ignore rule starts behaving as written. Not urgent this month.
- The README prose (line 60) references `gpt-tokenizer`, which is an unused dependency candidate. If it really is removed, update the README in the same change.
- `ENABLE_PROSODY` is referenced in code (`routes/assess.js:32`) and documented in README (line 108) but is a feature flag — no `.env.example` exists to drift against.
