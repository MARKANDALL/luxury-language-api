# Backend Monthly Hygiene Sweep — July 2026

## Summary
- .GOLD files: 11 total, 0 older than 60 days (delta from prior: first run)
- Unused dependency candidates: 1
- Stale branches: 0
- Env var drift: 5 missing from .env.example, 0 unused in code
- README mismatches: 0

## Findings

### 1. .GOLD backup cleanup candidates

Filesystem enumeration (`find . -name '*.GOLD*'`, excluding node_modules/.git/_ARCHIVE/.vercel) surfaced 11 backup files. All were created on the same day and are only ~1 day old, so none meet the 60-day cleanup threshold.

| Path | Size (KB) | Last modified | Age (days) |
|---|---|---|---|
| `index.html.GOLD` | 16 | 2026-06-30 00:55:46 | 1 |
| `admin/admin-label-user.js.GOLD` | 4 | 2026-06-30 00:55:46 | 1 |
| `routes/pronunciation-gpt.js.GOLD` | 20 | 2026-06-30 00:55:46 | 1 |
| `routes/migrate.js.GOLD` | 4 | 2026-06-30 00:55:46 | 1 |
| `routes/convo-report.js.GOLD` | 8 | 2026-06-30 00:55:46 | 1 |
| `routes/user-recent.js.GOLD` | 4 | 2026-06-30 00:55:46 | 1 |
| `routes/attempt.js.GOLD` | 8 | 2026-06-30 00:55:46 | 1 |
| `routes/update-attempt.js.GOLD` | 4 | 2026-06-30 00:55:46 | 1 |
| `routes/assess.js.GOLD` | 8 | 2026-06-30 00:55:46 | 1 |
| `routes/admin-recent.js.GOLD` | 8 | 2026-06-30 00:55:46 | 1 |
| `routes/convo-turn.js.GOLD` | 16 | 2026-06-30 00:55:46 | 1 |

Notes on the 2026-04-15 baseline: the reference list called out five `.GOLD.2` backups (`routes/attempt.js`, `migrate.js`, `pronunciation-gpt.js`, `update-attempt.js`, `user-recent.js`). None of those `.GOLD.2` files exist today — the naming pattern has shifted to plain `.GOLD`, and the set has expanded to cover six additional routes plus `index.html` and `admin/admin-label-user.js`. This is the first monthly report in `kodama-reports/backend-monthly/`, so no numeric delta is available.

✅ Nothing to clean up — all backups are fresh.

### 2. Unused dependency candidates

`node-wav` (package.json:23) is declared but has no `import`/`require` anywhere in the codebase. It is only referenced in `package.json`, `package-lock.json`, and one prose bullet in `README.md:59`. It is not a `@types/*` package, not in `vercel.json`, not in `package.json` scripts, and not a `@vercel/*` package.

All other dependencies verified in use:
- `@ffmpeg-installer/ffmpeg` → `routes/assess.js:5`
- `@google/genai` → `routes/convo-image.js`
- `@supabase/supabase-js` → `lib/supabase.js`
- `fluent-ffmpeg` → `routes/assess.js:4`
- `formidable` → `routes/assess.js`, `routes/evaluate.js`, `api/router.js`
- `jsonrepair` → `routes/pronunciation-gpt/json.js`, `runCoach.js`, `pronunciation-gpt.js`, `alt-meaning.js`
- `microsoft-cognitiveservices-speech-sdk` → `routes/tts/speak.js`, `routes/evaluate.js`, `routes/test-es-mx-phonemes.cjs`
- `openai` → `routes/convo-turn.js`, `pronunciation-gpt.js`, `alt-meaning.js`, `convo-report.js`, etc.
- `pg` → `lib/pool.js`
- `supertest` (dev) → four `test/*.test.js` files
- `vitest` (dev) → `vitest.config.js` + test files + `package.json` scripts

### 3. Stale branches (informational)

Only two remote branches exist:

- `origin/main` — last commit 5 weeks ago (~35 days). Under the 60-day threshold, and would be excluded anyway.
- `origin/claude/admiring-cori-6r8w9f` — this working branch.

✅ No stale branches to report.

### 4. Env var drift

**Missing from `.env.example` (used in code):**

| Env var | First reference |
|---|---|
| `GEMINI_API_KEY` | `routes/convo-image.js:9` |
| `FRONTEND_URL` | `routes/convo-image.js:40` (also `routes/convo-image-luma.js:11`) |
| `LUX_IMAGE_PROVIDER` | `routes/convo-image.js:48` |
| `LUMA_IMAGE_MODEL` | `routes/convo-image-luma.js:8` |
| `LUMA_API_KEY` | `routes/convo-image-luma.js:50` |

All five come from the newer `convo-image` / `convo-image-luma` routes and are not documented in `.env.example`. A fresh clone following the example file would silently miss image-generation configuration.

**Unused in code (present in `.env.example` but no `process.env.X` reference):** none. Every `.env.example` variable — including the commented-out aliases (`AZURE_REGION`, `AZURE_TTS_KEY`, `SUPABASE_SERVICE_ROLE`, `SUPABASE_SERVICE_ROLE_KEY_JWT`, `SUPABASE_SERVICE_KEY`, `SUPABASE_ANON_KEY`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `POSTGRES_CONNECTION`, `DATABASE_URL`, `PGSSLMODE`) — is referenced somewhere in `lib/` or `routes/`.

### 5. README accuracy

`README.md` Development section (lines 112–121) lists four commands:

| Command | Status |
|---|---|
| `npm install` | ✅ standard |
| `npm test` | ✅ matches `package.json` scripts.test (`vitest run`) |
| `npm run test:watch` | ✅ matches `package.json` scripts.test:watch (`vitest`) |
| `npm run hygiene` | ✅ matches `package.json` scripts.hygiene → `scripts/hygiene-report.backend.mjs` (file exists) |

✅ A user following the README would not hit an error.

## Notes
- The `.GOLD` backup set appears to have been regenerated wholesale on 2026-06-30 (single mtime across all 11 files, and the `.GOLD.2` pattern from the April baseline is gone). Worth confirming this was intentional — if a script rewrites backups every N days, none will ever cross the 60-day threshold and this check will always be silent.
- The `.env.example` gap is the only substantive finding. The five missing variables all belong to the image-generation surface and should be added (values commented as `your-*-here`) so operators can configure it without reading source.
